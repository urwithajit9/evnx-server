meta {
  name: List Vaults - No Auth (expect 401)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/vaults
  body: none
  auth: none
}

tests {
  test("status is 401 without token", function() {
    expect(res.getStatus()).to.equal(401);
  });
}